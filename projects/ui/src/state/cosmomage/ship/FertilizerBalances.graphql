query FertilizerBalances($account: String) {
  fertilizerBalances(where: { cosmomage: $account }) {
    amount
    fertilizerToken {
      id
      endBpf
      season
      humidity
      startBpf
    }
  }
}